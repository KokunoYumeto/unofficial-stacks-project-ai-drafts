# R49 independent build provenance review

Verdict: PASS for the bounded build-provenance checks described here. All 143 read-only checks passed. This review does not admit the candidate or assert visual or mathematical review.

The reviewed candidate is `stacks-errata-a04446e-r49`: five prior deRham units and seven source operations. The seven disjoint, hash-bound operations exactly reconstruct the payload from the authority source. P07-ERR-0003 remains excluded from this candidate. No new citation operation was inserted.

## Actual PDF and source bindings

| Object | Bytes | SHA-256 |
| --- | ---: | --- |
| Candidate PDF, both fresh runs and public build artifact | 843489 | `9AF52297946B9BF16231BE5F75D0D1F76B43829D0C8BCF5C9AD801219924B11E` |
| Authority PDF, both fresh runs and public build artifact | 843504 | `C8756EE3AB85C9B40B0AAE34DD07774A312D9C99ED39BB107B0DB263F8E1616C` |
| Candidate source | 230541 | `852B0FCC7F277D304C771F2E46A0A2EB54D6FEEBDB678A9B6CDEC3DA776C29C9` |
| Authority source | 230539 | `2DC4E936FCE0E3B2729F9D9B68D4FF5E6F188F1C25F6723D61B92FA682E90FC8` |

Raw PDF bytes were compared directly. The retained private source from each execution also matches the candidate source. The public second-run execution binds its actual outputs; its eight TeX/BibTeX command exit codes are zero and all retained command-output byte counts and raw hashes agree.

The first public execution file was overwritten by the second build. This review recovered its exact original bytes from the first run's raw outputs, dependency inventories, completion timestamp and the bounded whole-second interval for its start. Only one reconstruction matches the originally retained hash and byte count: 8100 bytes, `E0EE2BF36346B15531E546771AC084A1B4451A6F2742B6411250D9D5ED067522`. The recovered file records all eight command exits as zero and is retained as [RECONSTRUCTED_BUILD_A_EXECUTION.json](RECONSTRUCTED_BUILD_A_EXECUTION.json). It is explicitly a hash-verified reconstruction, not an original file that was independently retained.

## Final-pass dependency evidence

For candidate and authority in each of the two runs, the raw FLS was parsed anew. Its PWD, all 279 distinct ordered INPUT paths and all five distinct ordered OUTPUT paths match the inventory. All outputs are within the recorded isolated workroot. Canonical input closure hashes were recomputed rather than trusted from the existing receipt:

| Phase | Recomputed closure SHA-256 |
| --- | --- |
| Candidate, both runs | `3A5F397B315284362D2CC629F440DD4964424F4E688AEAE6291F4B277BD127C6` |
| Authority, both runs | `678CD388743CCF9D13DCDB10447290350A448A5BFD816BA406AE005F799483DB` |

All input rows and output rows agree between the two runs for each phase. For each inventory, 275 retained source/library inputs were independently rehashed with no mismatch. Four generated input files were removed with the temporary build trees: `derham.aux`, `derham.out`, `derham.toc`, and `derham.bbl`. Their inventory hashes and equality across executions are verified, but their original generated bytes cannot now be independently rehashed.

The public second-run log, FLS, final-pass stdout and BibTeX stdout are exact results of applying the inspected path sanitizer to their private raw counterparts. Public and private second-run inventory files are byte-identical.

This is explicitly final-TeX-pass-only FLS evidence. It does not establish every earlier-pass read or a complete BibTeX dependency closure. Source files and fonts/libraries represented by retained final-pass entries were checked; the reviewed evidence does not cryptographically pin the TeX/BibTeX executables themselves.

## Captured process tree and mutex

The inspected PowerShell wrapper acquires the exact named mutex `Global\InterlanguageTeXSlotV1` once, using a bounded 60-second acquisition. The C# guard creates each Python worker suspended, assigns it to an unnamed Job Object with KILL_ON_JOB_CLOSE and no breakaway flags, then resumes it. It waits for the entire captured job to reach zero active processes and confirms cleanup before the next job or mutex release. The code does not adopt foreign PIDs.

The receipt records acquisition at `2026-09-13T20:27:00.9134531Z` and release at `2026-09-13T20:27:32.4674610Z`. Both complete candidate/authority executions, deterministic comparison and immediate log/build preflight occur serially inside that same interval. Their captured process counts are 12, 12, 2 and 2; every root exit is zero, every cleanup is confirmed, and no termination or abandoned-mutex recovery is recorded. Raw guard stdout/stderr bindings were independently checked. The configured command timeout was 600 seconds.

The later captured receipt-binding job starts after release. Inspection of its script confirms that it reads and binds evidence without starting TeX or BibTeX. Its exit is zero, cleanup is confirmed, and its stdout binds the final build receipt hash.

This conclusion is supported by inspected code and hash-bound receipts. No new kernel event trace or fault-injection experiment was performed.

## Relative warnings and limits

Both candidate and authority PDFs are 68 pages. Fresh parsing of all four private final logs agrees with the public warning summaries: 374 unresolved reference occurrences over 288 distinct targets, zero unresolved citations, ten overfull horizontal boxes, and no underfull boxes, overfull vertical boxes, fatal markers or missing-glyph markers. Candidate and authority target multisets match exactly; every checked page/warning count delta is zero.

These are standalone chapter builds. Existing unresolved cross-chapter references and overfull boxes remain; matching the authority is not a zero-warning claim or a cumulative-book build. Visual adjudication remains a separate review.

All new files for this audit are confined to this review folder. No candidate, source, registry or Git mutation and no TeX/PDF build was performed. The machine verdict in [BUILD_INDEPENDENT_VERDICT.json](BUILD_INDEPENDENT_VERDICT.json) contains every check and the exact source, runner, public/private evidence and receipt bindings. The audit program is [audit_build.py](audit_build.py).
