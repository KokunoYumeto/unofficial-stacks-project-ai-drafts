# R45 finalization order

1. Materialize and independently replay the 19-unit, 36-operation source stage.
2. Build candidate and authority twice under the machine-wide TeX mutex using
   `-recorder`; bind deterministic PDFs, sanitized logs, and `.fls` closure.
3. Render every candidate page, inspect complete page coverage and every
   correction-sensitive page at high resolution, and preserve adverse evidence.
4. Freeze a final mechanical and visual stage receipt.
5. Obtain a separate exact-byte independent final replay of that frozen stage.
6. Seal `candidate.manifest.json` last, commit the candidate separately, then
   perform the registrar's append-only admission transition.

No step composes or pushes generated Stacks source.
