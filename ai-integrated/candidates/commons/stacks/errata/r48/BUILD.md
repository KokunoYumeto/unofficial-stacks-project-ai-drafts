# R48 build contract

The source stage is not admission. Execute two complete candidate/authority builds for every configured stem under Global\InterlanguageTeXSlotV1, using four new absent work/private roots. Work roots must be immediate children of the resolved temporary directory. Preserve actual failures. Run deterministic PDF and FLS comparison and immediate log checks before releasing the mutex. Confirm a captured-process-tree guard for every child before launching; the copied historical wrapper must be reviewed for that lifetime requirement. Then derive per-stem SyncTeX maps using payload line coordinates, render every page and sensitive pages at high resolution, obtain actual machine-recorded independent/visual decisions, run mechanical/prepare, independent final replay, and seal. Commit candidate, replay from a fresh checkout, then separately admit and release the lease.
